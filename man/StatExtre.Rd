% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/expostat.R
\name{StatExtre}
\alias{StatExtre}
\title{Extreme value calculation.}
\usage{
StatExtre(PID=res$PID, Group = T, Vars = "X5,X6,X7,X8,X9", LimitLow = 0.025, LimitUpper = 0.975, Layout = "column.points",
   Brightness = "light", Palette = "default2").
}
\arguments{
\item{PID}{chr. Program ID. It must be the same with the PID generated by InitStat.}

\item{OutPath}{chr. Output file directory, e.g. "D:/ExpoStat/StatExtre". It should be
noted that the slash symbol is "/", not "\".If "default", the current working directory will be set.}

\item{Group}{lgl. Whether to separate dataset into train and test data for normality test. The default is "TRUE".}

\item{Vars}{chr. Target variables used for modelling. It should be noted that there is fixed format for the entering characters
separated with  "," and without space. The default values is "all" (all variables are included).}

\item{LimitLow}{num. Lower limit ratio to screen the small extreme values located from 0 to this lower limit of the target variables.}

\item{LimitUpper}{num. Upper limit ratio to screen the large extreme values located from this lower limit to 1 of the target variables.}

\item{Layout}{chr. Visualization layout . Available values include "column.points", "heatmap".}

\item{Brightness}{chr. Visualization brightness . Available values include "light" and "dark".}

\item{Palette}{chr. Visualization palette . Available values include "default1", "default2", "default3" and 5  journal option
including "cell", "nature", "science", "lancet", "nejm".}
}
\value{
A list object containing the results of extremum for variables and  visualization of the results.
}
\description{
Extreme value calculation for numeric variables.
}
\examples{
res = InitStat()
   res1 = LoadStat(PID = res$PID, UseExample = "example#1")
   res2 = StatExtre(PID = res$PID, Group = T, Vars = "X5,X6,X7,X8,X9", LimitLow = 0.025, LimitUpper = 0.975, Layout = "column.points", Brightness = "light", Palette = "default2")
}
\author{
Yanqiu Feng, Bin Wang (corresponding author)
}
